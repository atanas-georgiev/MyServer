@model MyServer.Web.Areas.ImageGallery.Models.Album.SortFilterAlbumViewModel
@{
    ViewData["Title"] = LocString["Albums"];
}

<div class="well well-mobile">
    <div>
        <h1 class="text-center">@LocString["Albums"]</h1>
        <hr />
    </div>
    @Html.Partial("_SortFilterPartial", Model)
</div>

<div id="image-container">
    @await Component.InvokeAsync("AllAlbums", new { ViewDetailsUrl = "Album/Details/" })
</div>
    
